using SharedTypes;

namespace KinematicEngine.Core
{
    // All engine types are now defined in SharedTypes to avoid conflicts
    // This file is kept for backward compatibility but all types are imported from SharedTypes
} 